services:
  - type: web
    name: fiscal-felines
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: fastapi run app/main.py --port 8000
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ENV
        value: production